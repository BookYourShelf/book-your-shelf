spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/bys_db?useUnicode=true&useLegacyDatetimeCode=false&serverTimezone=Turkey
spring.datasource.username=user
spring.datasource.password=123456
spring.jpa.properties.javax.persistence.validation.mode=none
# The format used for the keystore. It could be set to JKS in case it is a JKS file
server.ssl.key-store-type=PKCS12
# The path to the keystore containing the certificate
server.ssl.key-store=classpath:keystore/test.p12
# The password used to generate the certificate
server.ssl.key-store-password=bookyourshelf
# The alias mapped to the certificate
server.ssl.key-store-provider=SUN
server.ssl.protocol=TLS
server.ssl.key-alias=test
server.ssl.enabled=true
server.port=8443
spring.jpa.properties.hibernate.event.merge.entity_copy_observer=allow
